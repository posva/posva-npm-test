{
  "name": "posva-npm-test",
  "version": "2.0.1",
  "description": "Personal testing environment",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "mkdir -p dist && cp readme.md dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "release-it": "^2.4.3"
  }
}
